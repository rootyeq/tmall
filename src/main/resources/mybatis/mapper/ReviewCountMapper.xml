<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 定义mapper接口路径 -->
<mapper namespace="com.login.tmall.dao.ReviewCountMapper">
    <select id="getReviewCount" resultType="review">
        select * from review where  review_product_id = #{product_id}
    </select>


</mapper>